[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rooom-vector"
version = "0.4.0"
description = "Small-team friendly vector database for RAG and Dify integration"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [{name = "ROOOMTECH"}]
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "numpy>=2.0",
  "pydantic>=2.8",
  "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
gpu = ["cupy-cuda12x>=13.0; platform_system == 'Linux'"]

[project.scripts]
rooom-vector = "rooomtech_vector.api:run"
rooomtech-vector = "rooomtech_vector.api:run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
